services:
  # ── Backend (FastAPI) ──────────────────────────────────────────────────────
  - type: web
    name: recruitiq-v4-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false          # set manually in Render dashboard
      - key: ALLOWED_ORIGINS
        sync: false          # set to your frontend URL e.g. https://recruitiq-v4.onrender.com

  # ── Frontend (React/Vite static site) ─────────────────────────────────────
  - type: static
    name: recruitiq-v4-frontend
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        sync: false          # set to your backend URL e.g. https://recruitiq-v4-backend.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
